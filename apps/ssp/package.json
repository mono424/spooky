{
  "name": "ssp",
  "version": "0.0.1-canary.226",
  "private": true,
  "scripts": {
    "build": "docker buildx build --target ssp -t mono424/spooky-ssp:dev -f ../../Dockerfile ../..",
    "dev:build": "cargo build -p ssp-server"
  }
}
