---
layout: ../../../layouts/DocsLayout.astro
title: Cloud Deployment
---

import CodeBlock from '../../../components/ui/CodeBlock.astro';
import Note from '../../../components/ui/Note.astro';

export const code1 = `spky <COMMAND>

Develop:
  init               Scaffold a new project
  dev                Start a local dev environment
  generate           Generate client types
  migrate            Database migrations
  recipe             Render a cookbook recipe

Deploy & operate:
  deploy [--upgrade] Deploy the current project
  status             Show deployment status
  logs [--filter]    Tail deployment logs
  restart [--clean]  Restart scheduler/SSP containers (data preserved)
  scale ssp <N>      Scale SSP instances
  login / logout     Authenticate with Sp00ky Cloud

Resources:
  env                Environment variables + the encryption vault
  domain             Custom domains
  backup             Database backups
  link               GitHub auto-deploy

Account:
  project            Create, list, credentials, destroy
  team               Members and invitations
  billing            Billing management
  token              API tokens for CI/CD`;

This page has been reorganized. See:

- [Getting Started](/docs/cloud/getting-started) — Authentication, project creation, billing
- [Deploying](/docs/cloud/deploying) — Deploy command, upgrades, status, scaling
- [Logs & Monitoring](/docs/cloud/logs) — Log tailing, filtering, status badge
- [CI/CD](/docs/cloud/ci-cd) — API keys, GitHub integration

## CLI Reference

<CodeBlock
  code={code1}
  lang="bash"
/>
