---
layout: ../../../layouts/DocsLayout.astro
title: Getting Started
---

import CodeBlock from '../../../components/ui/CodeBlock.astro';
import Warning from '../../../components/ui/Warning.astro';
import Note from '../../../components/ui/Note.astro';

export const code8 = `# 1. Log in to Sp00ky Cloud
spky login

# 2. Create a cloud project
spky project create --slug my-app

# 3. Deploy
spky deploy`;

export const code7 = `spky login`;

export const code6 = `spky logout`;

export const code5 = `# Interactive — prompts for slug if not provided
spky project create

# With options
spky project create --slug my-app --plan starter`;

export const code4 = `# sp00ky.yml
slug: my-app
# ... rest of config`;

export const code3 = `spky project list`;

export const code2 = `spky project destroy`;

export const code1 = `# Open the billing portal
spky billing

# View current month's usage
spky billing usage`;

Sp00ky Cloud handles infrastructure provisioning, scaling, and management so you can deploy your project with a single command. No Docker Compose files, no server setup — just `spky deploy`.

## Prerequisites

- The Sp00ky CLI installed (`pnpm add -D @spooky-sync/cli` per project, or `npm i -g @spooky-sync/cli` globally)
- A `sp00ky.yml` config file in your project root
- Docker installed locally (for building backend images)

## Quick Start

<CodeBlock
  code={code8}
  lang="bash"
/>

## Authentication

Log in via GitHub OAuth. The CLI opens your browser to complete the flow:

<CodeBlock
  code={code7}
  lang="bash"
/>

A 4-digit verification code is shown in your terminal. Confirm it in the browser to authenticate. Credentials are saved to `~/.sp00ky/credentials.json`.

To log out:

<CodeBlock
  code={code6}
  lang="bash"
/>

<Note title="CI/CD">
For automated pipelines, create an API key with <code>spky token create</code> and set the <code>SPOOKY_API_KEY</code> environment variable. See [CI/CD](/docs/cloud/ci-cd) for details.
</Note>

## Creating a Project

<CodeBlock
  code={code5}
  lang="bash"
/>

Available plans: `starter`, `pro`, `business`. The slug must be lowercase alphanumeric with hyphens, and is globally unique.

Your project slug should match the `slug` field in your `sp00ky.yml`:

<CodeBlock
  code={code4}
  lang="yaml"
/>

## Listing Projects

<CodeBlock
  code={code3}
  lang="bash"
/>

Shows all your cloud projects with their plan, status, and creation date.

## Destroying a Project

<Warning>
This permanently tears down all VMs and infrastructure for the project. Data will be lost unless you have a backup.
</Warning>

<CodeBlock
  code={code2}
  lang="bash"
/>

## Billing

<CodeBlock
  code={code1}
  lang="bash"
/>
