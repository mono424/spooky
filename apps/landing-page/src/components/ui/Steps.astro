---
/**
 * Numbered procedure list. Children must be <Step> elements, numbering comes
 * from a CSS counter in docs.css, so no index has to be threaded through.
 */
---

<ol class="docs-steps">
  <slot />
</ol>
