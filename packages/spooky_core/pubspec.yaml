name: spooky_core
description: Pure-Dart core for Spooky local-first sync (framework-agnostic).
version: 0.0.1-canary.195
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  ffi: ^2.1.0
  sqlite3: ^2.4.0
  crypto: ^3.0.3
  uuid: ^4.4.0
  web_socket_channel: ^3.0.0
  logging: ^1.2.0
  meta: ^1.12.0
  yaml: ^3.1.2 # codegen: parse OpenAPI backend specs
  cbor: ^6.5.1

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
  fake_async: ^1.3.1
