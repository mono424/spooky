# AGENTS.md

> Auto-generated by `spky agents init` — re-run after schema changes to refresh.

## What this app uses

**sp00ky** — a reactive, local-first SurrealDB sync framework. The schema in `./schema/src/schema.surql` is the source of truth; running `spky generate` produces typed `schema.gen.ts` (and Dart) bindings that the app imports. Local mutations are optimistic and drain through SSP to the remote SurrealDB.

This is not Mastra/LangChain/LangGraph (not an agent runtime), and it is not Convex/PowerSync (different sync model — sp00ky uses SurrealDB live queries + a local DBSP circuit).

## Mental model

```
.surql schema  ──spky generate──►  schema.gen.ts (typed)
                                        │
                                        ▼
                            SyncedDbConfig + Sp00kyProvider
                                        │
                            useDb() → db.create / db.update / db.delete / db.query
                                        │
                local cache (memory|IndexedDB)  ↔  SSP  ↔  remote SurrealDB
```

## Your tables

### `collaborates_on`

_(no CRDT, parent, or relationship annotations)_

### `comment`

**Parent fields** (auto-populated server-side from auth context — do not write from client code):
- `comment.thread` → `thread`

**Record links:**
- `comment.author` → `user`

### `commented_on`

_(no CRDT, parent, or relationship annotations)_

### `thread`

**CRDT fields** (use `useCrdtField`, not `useQuery`; pair writes with `{ debounced: true }`):
- `thread.content` (`@crdt text`)
- `thread.title` (`@crdt text`)

**Parent fields** (auto-populated server-side from auth context — do not write from client code):
- `thread.author` → `user`

### `thread_invite`

**Record links:**
- `thread_invite.created_by` → `user`
- `thread_invite.thread` → `thread`

### `user`

_(no CRDT, parent, or relationship annotations)_

## Generated outputs

- `./app-solid/src/schema.gen.ts` — TypeScript
- `./client-dart/src/db/models.dart` — Dart

## Where to look

- `node_modules/@spooky-sync/core/AGENTS.md` — sync engine, mutation queue, CRDT manager
- `node_modules/@spooky-sync/client-solid/AGENTS.md` — `useDb`, `useQuery`, `useCrdtField`, `Sp00kyProvider`
- `node_modules/@spooky-sync/query-builder/AGENTS.md` — typed query DSL
- `node_modules/@spooky-sync/cli/AGENTS.md` — `spky` subcommands and schema annotations
- `node_modules/@spooky-sync/devtools-mcp/AGENTS.md` — live MCP introspection

## Common gotchas

1. **Run `spky generate` after every schema edit.** Then `spky doctor` to confirm nothing else drifted.
2. **Record IDs are full strings.** `db.create('thread:abc', {...})`, not `db.create('thread', {id: 'abc'})`. Mint IDs with `new Uuid().toString()`.
3. **CRDT fields need `useCrdtField`.** Reading them via `useQuery` returns the last raw merged value, not a reactive Loro doc.
4. **`@parent` columns are read-only from the client.** They're populated server-side from the auth context.
5. **`useQuery` factories must end with `.build()`.** A bare builder hangs the hook.
6. **`db.useRemote(fn)` bypasses the local cache.** Use only for queries the type-safe builder can't express.

## Agent feedback loop

After any schema or config change:

```bash
spky generate          # refresh schema.gen.ts
spky doctor --json     # structured diagnostics — parse-and-grep ready
```

## Cookbook recipes

Render a parameterized snippet for any of these:

- `spky scaffold live-list --table <table>` — reactive list view
- `spky scaffold optimistic-mutation --table <table>` — create-record form
- `spky scaffold crdt-text-field --table <table> --field <field>` — collaborative editor

Run `spky scaffold list` for the full index, or read `node_modules/@spooky-sync/cli/templates/cookbook/INDEX.md`.
